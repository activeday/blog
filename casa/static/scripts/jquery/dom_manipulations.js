

/*function isModalOpen(){
	if($('#myModal').is(':visible'))
		return true;
	else
		return false;


var county_select = $('select[data-counties="true"]');
var county_modal_select = $('select[data-modal-counties="true"]');

/*function populateCounties(county){
   county_select.append(
    	"<option>" + county + "</option>"
    );
   county_modal_select.append(
   		"<option>" + county + "</option>"
   	);
}

counties.map(populateCounties);

$('button[data-type="image-upload"]').click(function(clicked){
	clicked.preventDefault();
	$('input[type="file"]').trigger('click');
});

function imagePreview(input){
	if(input.files && input.files[0]){
		var reader = new FileReader();

		reader.onload = function(e){
			$('img[data-upload="listing-image"]').attr('src', e.target.result);
		}

		reader.readAsDataURL(input.files[0]);
	}
}

$('input[type="file"]').on('change', function(){
	imagePreview(this);
});

$('button[data-type="login"]').click(function(clicked){
	clicked.preventDefault();

	$('form[data-type="loginform"]').trigger('submit');
});

$('button[data-type="createaccount"]').click(function(clicked){
	clicked.preventDefault();
	console.log('boom');
	$('form[data-type="signupform"]').trigger('submit');
});
*/
